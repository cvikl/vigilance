# Rebuild runbook (day of hackathon)

Ordered log of what was built, one line per task, so the product can be re-created from scratch in the build window. Append as you go.

## Rebuild day — ordered restore (2026-09-18)

Fast path: code from GitHub, data from Drive, no model server until the very last step. Every archive on Drive was checksummed on upload (`SHA256SUMS-2026-09-18.txt`, `REBUILD-MANIFEST-2026-09-18.md` in the Drive folder); protocol hash `83174236e120` is already locked in the repo — **never re-run `protocol lock`** (it would orphan every archived table). Times measured on this host.

1. **Code** (≈ 2 min): `git clone <github> auditpace_project && cd auditpace_project` (fallback with no GitHub: `git clone rebuild-extras/extras/auditpace-git-2026-09-18.bundle`). `uv venv --python 3.12 && uv sync --extra dev` (copy `extras/uv.lock` to the repo root first for the exact resolution). `uv run auditpace --version`; `uv run auditpace protocol validate`.
2. **Drive pull** (≈ 2 GB, bandwidth-bound): rclone remote `auditpace` (read-only, `docs/setup.md`) then `scripts/sync_drive.sh` → `data/raw/*.{zip,tar.gz}` + `docs/drive/`. Verify: `cd data/raw && sha256sum -c ../../docs/drive/SHA256SUMS-2026-09-18.txt` (the Synthea zip is not in that list; it is unchanged since 2026-09-12).
3. **Synthea** (≈ 1 min): `mkdir -p data/synthea && unzip -q data/raw/100k_synthea_covid19_csv.zip -d data/synthea/` → `data/synthea/100k_synthea_covid19_csv/*.csv` (path in `config.yaml`). `uv run auditpace cohort --force` (≈ 10 s) → `uv run python scripts/cohort_sanity.py`. (Deterministic, seed 42 — same `cases`/`patients`/`events` the archives were built from.)
4. **Archives** (seconds each): `for a in documents pages readings verdicts; do tar xzf data/raw/$a-83174236e120.tar.gz -C data/processed/; done` then `tar xzf data/raw/reviews-2026-09-18.tar.gz -C data/processed/` (the 150-review rehearsal seed; or regenerate: `uv run python scripts/seed_reviews.py --per-criterion 25 --seed 42 --wipe`, 13 s). Optional `tar xzf data/raw/processed-small-2026-09-18.tar.gz -C data/processed/` restores yesterday's S6 parquets, `auditpace.duckdb` and the S8 `report/` run dirs (only needed if you want `--compare-to` a 2026-09-17 run; everything in it is regenerated by steps 3 and 6). Tessdata: `scripts/fetch_tessdata.sh` (GitHub) or copy `extras/eng.traineddata` to `data/tessdata/`.
5. **Sanity gates** (≈ 1 min total, no model): `uv run python scripts/synth_sanity.py` → `ok`; `scripts/render_sanity.py` → `ok`; `scripts/read_sanity.py` → `ok`; `scripts/adjudicate_sanity.py` → `GATES: ok`.
6. **S6** (4 s): `uv run auditpace estimate --coverage` → `scripts/estimate_sanity.py` → `GATES: ok` (small-m cells print `provisional`).
7. **S7** (3 s to first response): `nohup uv run auditpace workbench --port 8080 > logs/workbench.log 2>&1 &`; `curl -s http://127.0.0.1:8080/healthz` → `"reviews": 150`. Demo: `http://127.0.0.1:8080/queue?demo=1`; beats and case ids in "S7 workbench" below and `docs/demo_cases.txt`.
8. **S8 fallback first** (4 s): `uv run auditpace report --no-model --out docs/report.fallback.md && cp docs/report.fallback.md docs/report.md`; `uv run python scripts/report_sanity.py` → `GATES: ok`; Report tab shows 9 `<h2>`. The pitch can run from here with no GPU at all.
9. **S8 with MedGemma** (≈ 10 min incl. serve): HF licence + `hf auth login` (human, once); `hf download google/medgemma-27b-text-it` if the cache is cold (≈ 51 GB); `uv pip install -e ".[serve]"` (≈ 10 min, once); `ONLY=27b make serve` (shared GPU: `UTIL27=0.66 MAXLEN27=12288 EAGER27=1 make serve`), `make check` until `reporter: ok`; `AUDITPACE_MOCK=0 uv run auditpace report` (≈ 2 min) — summary line must say `model_unavailable=0`; if it says 1 or the prose reads wrong, `cp docs/report.fallback.md docs/report.md`. Kill the server when done (`pkill -f "vllm serve"`).
10. **Tests** (7–8 min, optional on the day): `uv run playwright install chromium` then `make test` → **469 passed, 0 skipped**; `make lint` clean.

**Dry run 2026-09-18 08:19–08:23 (fresh clone into a sibling dir, steps 1–8, no GPU): passed end to end in ≈ 4 min** — `uv sync` ≈ 40 s, Drive pull 2.0 GB in 22 s, checksums 7/7 OK, unzip + `cohort --force` + `cohort_sanity` 36 s, four archives + tessdata 14 s, four sanity gates 24 s, `estimate --coverage` 7 s, workbench `/healthz` `reviews: 150` in 4 s, `report --no-model` + `report_sanity` + Report tab 9 `<h2>` in 10 s. One defect found and fixed on the spot (`983d0d3`): `scripts/synth_sanity.py` read `docs.style`, which became pandas' Styler accessor once S8 added jinja2 — now `docs["style"]`. Steps 9–10 (27B report, `make test`) were not part of the dry run.

Recompute paths (if an archive is unusable) are in each stage section: S2 ≈ 75 min of `claude -p`, S3 ≈ 25–80 min, S4 ≈ 2 h (4B on :8002), S5 ≈ 2 h (27B on :8003). S6–S8 are always recomputed. Never point the two vLLM servers at the same GPU.

## Demo day (2026-09-18) — rehearsal and pitch

Product name on stage: **vigiLANCE** (code, CLI, repo stay `auditpace`). Plan and tick-list: [superpowers/todo-s9-s10.md](superpowers/todo-s9-s10.md); words: [pitch.md](pitch.md); deck handover: [pitch/cowork-brief.md](pitch/cowork-brief.md).

- **Reset to the rehearsal state** (≈ 30 s): `make demo-reset` — stops :8080, `seed_reviews.py --per-criterion 25 --seed 42 --wipe` (demo cases and the held arrival excluded), `estimate --coverage`, restarts the workbench, prints `/healthz` (`reviews: 150, held: 1`). Run it before every rehearsal and before the pitch. No model server needed.
- **Open** `http://127.0.0.1:8080/queue?demo=1` on the projector screen; click **Pitch assistant** (bottom-right) — the sheet holds the audit block, progress bar, C1–C5 checklist and `held: 1 (n to release)`.
- **Driver's beats**: `docs/demo_script.md` (locked words, clicks, clock, fallbacks). Shape (≈ 80 s): Latest row 3 (abstain, pointed at) → row 1 (legitimate wait) → `n` lands the stalled H4, open via the why-this-row text, Next step card, Confirm → Results & alerts card 1 + organisation table → Audit report. Check first and the `9f786` H5 override are held for Q&A.
- **Report tab** shows `docs/report.md` written live by 27B at 10:33 (`model_unavailable=0`, 2 of 21 paragraphs withheld by the back-check). `docs/report.fallback.md` is the `--no-model` twin; `cp` it over if anything reads wrong. Both gitignored — regenerate with `AUDITPACE_MOCK=0 uv run auditpace report` (27B up) or `uv run auditpace report --no-model --out docs/report.fallback.md`.
- **Held arrivals** live under `## held` in `docs/demo_cases.txt`; the server hides them at start, `POST /demo/release` reveals one, `POST /demo/reset` re-holds all (a restart does too). Released rows carry `data-new` for 60 s.
- **PPI figure** for the tech slide: `uv run python scripts/ppi_figure.py` → `docs/pitch/ppi_figure.{svg,png,md}` (≈ 40 s; reads `cases.breached`, evaluation only).

## S0 scaffold
- T1: `uv venv --python 3.12`; pyproject (typer, pydantic, pyyaml, duckdb, pyarrow, pandas, httpx); `src/auditpace/cli.py` with `--version`.
- T2: `config.yaml` + `settings.py` (paths, model endpoints, seed, AUDITPACE_MOCK).
- T3: `protocol.yaml` (6 handoffs) + `protocol.py` (pydantic schema, canonical sha256, lock, require_locked); `auditpace protocol validate|lock`.
- T4: `store.py` — Store(processed_dir): write/read parquet, DuckDB views, sql().
- T5: `stage.py` — Stage base (items/process/is_done), run(limit, force), quarantine jsonl, RunSummary.
- T6: `scripts/make_mini_fixture.py` → `tests/fixtures/mini/synthea/*.csv` (5 patients); conftest fixtures `mini_settings`, `locked_protocol`.
- T7: `models.py` — ModelClient(chat, chat_json, images as data-URI), cache_key, mock replay from fixtures/responses, AUDITPACE_RECORD=1 records; client_for(settings, role).
- T8: `scripts/serve_medgemma.sh` (vLLM 27B :8003, 4B :8002 — each pinned to one GPU via `scripts/pick_gpu.sh`, no tensor parallelism); `auditpace models check`. Human: HF licence + hf auth login.
- T9: Makefile (test/lint/serve/check/fixture), GitHub Actions CI (mock tests), README.
- F1: `store.py` — resolve `processed_dir`, validate table names (`_check_name`/`_NAME_RE`) in `write()`/`path()`, ignore non-identifier parquet stems on init.
- F2: `protocol.py` `canonical_hash` uses `exclude_defaults=True` so adding a new defaulted field to the schema can't change the locked hash.
- F3: `stage.py` opens the quarantine file with `"w"` (not `"a"`) so it reflects only the latest run, not every run ever.
- F4/F5: `scripts/pick_gpu.sh` (least-`memory.used` GPU among 0,1,2, excluding given indices) + `src/auditpace/gpu.py` (`pick_gpu`/`pin_gpu`); `scripts/serve_medgemma.sh` now pins each MedGemma process to exactly one GPU (no tensor parallelism) and fixes `--limit-mm-per-prompt` to valid JSON.
- F6: `Makefile` `test` target runs with `AUDITPACE_RECORD=` so `make test` can never hit a real model endpoint.
- F7: `models.py` `cache_key` docstring documents that temperature/max_tokens are excluded from the cache key by design.
- F8: added `test_is_done_skips_unless_force` (stage.py) and `test_require_locked_rejects_edited_after_lock` (protocol.py).
- F9: confirmed B008 is an enabled ruff rule that fires on `cli.py`'s `typer.Argument(...)` defaults; kept `extend-ignore = ["B008"]`.
- F10 (2026-09-14, S4 prep): `uv pip install -e ".[serve]"` → vllm 0.29.0 / torch 2.13.0+cu130 / transformers 5.17.0 (~10 min). Human: accept licence for `google/medgemma-1.5-4b-it` and `google/medgemma-27b-text-it`, read token, `HF_HOME=/data/$USER/.cache/huggingface uv run hf auth login --token …` (system `/etc/profile.d` sets `HF_HOME` to another user's read-only cache — `Makefile` and `serve_medgemma.sh` now export a per-user default). `uv run hf download google/medgemma-1.5-4b-it` (8.1 GB, ~20 s via xet). `serve_medgemma.sh` gains `ONLY=4b|27b|both` and `UTIL27`/`UTIL4` (GPUs are shared; 4B default 0.50); `ONLY=4b make serve` → :8002 ready in ≈ 3 min. Smoke: one page → transcript ≈ 4.5 s, ~475 tokens, deterministic at temperature 0; the very first request after boot once produced a 9.5k-char repetition loop (not reproducible) — S4 must cap `max_tokens` and treat `finish_reason == "length"` as a per-page failure.

## S1 cohort
- T1: `cohort/source.py` SyntheaSource — DuckDB views over CSVs, dates parsed.
- T2: `cohort/select.py` select_patients — cohort rule + seeded stratified sample (icu, died).
- T3: `cohort/events.py` derive_events (first occurrence, after-rules, has_time), organisation_of.
- T4: `cohort/cases.py` build_cases — patient×criterion grid, hours, breached, applies.
- T5: `cohort/plant.py` plant_truth — per-case seeded breach draw, times-of-day, lognormal breach magnitude, true_reason.
- T6: `cohort/stage.py` CohortStage + `auditpace cohort [--limit] [--force] [--protocol]`; writes patients/events/cases/cohort_completeness.
- T7: `auditpace protocol lock` (now patches locked:/hash: lines in place instead of round-tripping through yaml.safe_dump, so SNOMED-code comments survive) then `auditpace cohort --force` on full Synthea (1,500 pts, ~10s); `scripts/cohort_sanity.py` checks planted breach rates per criterion (all within 0.08 of `synthetic.breach_rates`). Fix: `cohort/select.py::_eligible` now applies `cohort.period` to the `require_any` encounter's START too (not just the condition), so a stray out-of-period inpatient stay no longer makes a patient eligible; `admitted` went from 1000/1500 to 1416/1500 (~94%).
- Final-review fix wave (F1-F2, M1-M4): Synthea's date-only fields (medications, procedures) are local dates while encounter timestamps are UTC, so a date-only end can precede a timed start; `plant_truth` leaves such ends unclamped rather than collapsing them to the 15-minute floor (F1). Open design decision for S2: `cases.start_ts`/`end_ts` are per-case planted times — the same event (e.g. `admitted`) can carry a different planted time in different cases (once per criterion the event participates in) — S2 must decide a precedence rule when reconciling event times across cases for the same patient; this is not a bug.

## S2 synth
- T1: `cohort/plant.py` plant_timeline — one planted time per (patient, event), `events.planted_ts`; cases derived from the timeline; `discharged` guarded ≥ last planted in-stay event + 1 h. Re-ran `auditpace cohort --force`.
- T2: Store partitions — write_part(name, part, df) → processed/<name>/<part>.parquet; read/sql/exists resolve either form; connection use serialised by a lock.
- T3: Stage.run(workers=) — thread pool over process(); outputs/quarantine in item order.
- T4: models.ClaudeCLI — claude -p subprocess (stdin prompt, --json-schema, --tools "", empty cwd) with ModelClient's record/replay fixtures; Settings.synth {model, batch_size, workers, timeout_s}.
- T5: synth/facts.py + reasons.yaml (≥3 clinician phrasings per criterion×reason) + distractors.yaml; Fact dataclass; seeded picks by stable_int.
- T6: synth/plan.py build_plan — six doc types anchored on events (+seeded offsets), carrier tables keyed by criterion `to` event, reason on breached / 30% distractor on unbreached, 20% handwritten notes, death cap.
- T7: synth/prompt.py (SYSTEM_PROMPT, PROMPT_VERSION, OUTPUT_SCHEMA, aliased JSON batch prompt) + synth/validate.py (coverage, verbatim facts, banlist, 60–600 words).
- T8: synth/stage.py SynthStage (batches of 5 sorted patients, plan→prompt→ClaudeCLI→validate, ≤3 attempts, one parquet partition per batch, is_done by planned doc_ids) + `auditpace synth [--limit] [--force] [--workers] [--batch-size]` (exit 2 without cohort, 1 on quarantine).
- T9: recorded mini fixture (`tests/fixtures/mini/responses/`), `scripts/synth_sanity.py`. Fix: real `claude -p` recordings kept continuing a sentence past a `must_include` fact's final full stop (never rewording the fact itself) — `validate.py`/spec §8 now strip a fact's terminal punctuation before the verbatim check, `prompt.py` SYSTEM_PROMPT now says this is allowed. Fix: `synth/plan.py` compared `died` (not `died_cap`) when deciding whether to drop a patient's `clinic_letter`, so a same-calendar-day death/follow-up (Synthea death dates are date-only) dropped the carrier a still-"applies" H6 case needed — crashed planning for 13/1500 cohort patients; now uses `died_cap` consistently. Smoke run `AUDITPACE_MOCK=0 uv run auditpace synth --limit 2 --workers 2` authored 45 documents for 10 patients, `scripts/synth_sanity.py` → `ok`. Full run `AUDITPACE_MOCK=0 uv run auditpace synth --workers 8` (~300 calls, ~75 min) → `data/processed/documents/b*.parquet` (~6.7k docs) to be run separately by the controller. Resumable; re-run skips finished batches.
- Full run: `AUDITPACE_MOCK=0 nohup uv run auditpace synth --workers 8 > logs/synth_full.log 2>&1 &` (~300 calls; observed ~2.8 batches/min → ~2 h). Resumable; `--force` re-authors. Check: `uv run python scripts/synth_sanity.py` → `ok`.
- Archive after the run: `tar czf documents-$(cut -c1-12 <<< "$(grep '^hash:' protocol.yaml | cut -d' ' -f2)").tar.gz -C data/processed documents quarantine` → upload to the Drive folder by hand (the `auditpace` rclone remote is read-only). Restore on rebuild day: `scripts/sync_drive.sh` pulls the archive into `data/raw/`; `tar xzf data/raw/documents-<hash12>.tar.gz -C data/processed/`; then `scripts/synth_sanity.py` must print `ok`. Run of 2026-09-13/14: prompt_version `synth-0f973d392a9b`, 6,361 documents (1,500 patients; lab_report 1500, ed_clerking 1416, ward_round 1416, discharge_summary 1385, clinic_letter 363, icu_note 281), 0 batches quarantined, `synth_sanity.py` ok. Archive `documents-83174236e120.tar.gz` (in `data/raw/`, upload to Drive by hand). Note: `claude -p` calls count against the Claude session limit (~73k cached input tokens per call); the 300-batch run tripped the limit three times and was resumed each time with the same command.
- Final-review fix wave: `cohort/plant.py` draws a date-only planted end uniformly inside the Synthea day on the drawn side of the target instead of clamping to 00:00/23:59 (boundary share 24% → <0.1%); `cohort/events.py::drop_post_mortem` removes Synthea events dated after the death day before cases are built (post-death H6 ends 208 → 7, the rest are timed planted ends that overrun death and get no facts per spec §5); `synth/plan.py` authors `ward_round` at max(admitted, enoxaparin) + 1–24 h and `ed_clerking` always before `icu`, rounds `authored_ts` up to the minute, and keys the distractor draw `<case_id>:distractor_draw`; `synth/validate.py::fact_needle` is the S5 verbatim contract; SYSTEM_PROMPT names the 2-week respiratory clinic review (prompt_version changed, mini fixture re-recorded); `ClaudeCLI` reuses one empty cwd per instance; stage prints flush.
- Open for S5/S6: 782/1416 H6 cases have no `follow_up` (hours null, breached false, no clinic_letter) — decide abstain vs "no follow-up documented = breach".

## S3 render
- T1: deps `playwright`, `pillow`, `jinja2`; one-off `uv run playwright install chromium` (no sudo needed on this host — Chromium's shared libs are present; ~120 MB into `~/.cache/ms-playwright`). Fonts committed under `src/auditpace/render/assets/fonts/` (Caveat OFL, DejaVu Serif/Sans) and embedded as base64 `@font-face` by `render/fonts.py`. `Settings.render {workers}`. `Store(..., read_only=True)` uses an in-memory DuckDB so scripts can read while a stage writes.
- T2: render/layout.py — blank-line blocks (title / fields / para), every str.split() token wrapped in <span class="w">; word contract = text.split().
- T3: render/assets/templates/*.j2 + render/templates.py — base page (1240×1754, letterhead, footer) + six doc_type children; handwritten = Caveat 32px on ruled lines; render_html(meta, body, page_no, page_count).
- T4: render/browser.py Renderer — one headless Chromium page, render(html) → PNG + Word rects + block extents; tests skip when Chromium is missing.
- T5: render/paginate.py (greedy whole-block pages, OverflowError names the block) + render/document.py render_document — one render when it fits, else re-render per page; words partition exactly.
- T6: render/artefacts.py — seeded rotate ±1.5°, blur σ≤0.8, noise σ 2–8, JPEG q 60–80; `apply` returns those JPEG bytes directly (no PNG re-encode — the noise makes the pixels incompressible, so re-encoding to PNG only inflated the archive). transform_words rotates boxes about the page centre (verified sign), rounds outward, clips.
- T7: render/stage.py RenderStage (one batch = one documents partition; one Renderer per batch; pages/<batch>.parquet + pages/<batch>/<doc_id>_p<n>.jpg; is_done by doc_id set + RENDER_VERSION + `text_sha` (sha256(documents.text)[:12], detects a document re-authored in place even when doc_id set/RENDER_VERSION are unchanged); stale partitions refused without --force) + `auditpace render [--limit] [--force] [--workers]` (exit 2 without documents, 1 on quarantine). `pages` column is `image_path` (was `png_path`). `mini_store` fixture moved to tests/conftest.py.
- T8: `scripts/render_sanity.py` (read-only store; word contract, box/ink ≥ 0.99, a box-shape gate — per style, max sampled word-box height ≤ 1.5× that style's median — catches Chromium soft-wrapping a hyphenated token like `SARS-CoV-2` at a line end, which otherwise makes `getBoundingClientRect` return a two-line union box; prints `max box height typed=<n> handwritten=<n>`; samples in `logs/render_samples/`). Smoke: `uv run auditpace render --limit 2 --workers 2`. Full run: `nohup uv run auditpace render --workers 4 > logs/render_full.log 2>&1 &` (~300 batches, ~12 min; resumable; `--force` re-renders ALL batches, required after `auditpace synth --force` — though a re-authored document is now also auto-detected via `text_sha`, without needing `--force`, unless `RENDER_VERSION` or `doc_id` set also changed).
  - Final-review fix wave: added `.w{white-space:nowrap}` to `base.html.j2` (Critical — the hyphen-wrap box defect above affected ≈ 18% of pages before the fix); `RENDER_VERSION` now also hashes page geometry (`templates.GEOMETRY`) and the fields-line regex (`layout.FIELD_LINE_PATTERN`) so a change to either correctly invalidates old pages; per-doc_type typed font sizes moved from the `.j2` templates into `templates.py` (`TYPED_PX`/`TYPED_LH`, `SIZES`); `gt_words` serialised via `dataclasses.asdict`; `process()` clears a leftover quarantine directory before re-rendering a batch. `playwright==1.62.0` (chromium build `chromium-1234` under `~/.cache/ms-playwright`), `pillow==12.3.0`.
  - Full re-render of 2026-09-14 (post-fix): render_version `render-81533844d295`, 6,388 pages / 6,361 documents (300/300 batches), pages-per-document `{1: 6334, 2: 27}`, words per page min=23 median=185 max=298, doc_type×style: lab_report typed 1500, ed_clerking typed 1103/handwritten 340, ward_round typed 1135/handwritten 281, icu_note typed 281, discharge_summary typed 1385, clinic_letter typed 363. First pass: 1 batch (`b0000`) transiently quarantined — `Page.screenshot: Protocol error (Page.captureScreenshot): Unable to capture screenshot`, a one-off Chromium hiccup from 4 browsers launching concurrently at the start of the run, not a code defect; a plain re-run (no `--force` needed — `b0000` simply wasn't `is_done`) resumed and rendered just that batch, `render: in=300 out=300 quarantined=0`. Duration: full pass 19:55→21:15 (≈ 80 min with `--workers 4`; well above the design-spec's ≈ 12 min estimate and the previous pre-fix run's ≈ 25 min — host was under heavier concurrent load this run) plus the ≈ 1 min resume. `scripts/render_sanity.py` → `ok` (boxes-with-ink 1.000 on a 200-page sample; max box height typed=35 handwritten=51 — typed's max comes from bold 1.15em `.title` block words, e.g. "RESPIRATORY", "DISCHARGE", not a residual wrap: the full typed height histogram is smooth from 25–35 px with no bimodal spike, and both a typed and a handwritten sample image were inspected — no wrapped hyphenated tokens visible). Archive: `tar czf data/raw/pages-83174236e120.tar.gz -C data/processed pages` → `pages-83174236e120.tar.gz` in `data/raw/`, **1,400,202,301 bytes (1.40 GB / 1.30 GiB)** — far smaller than the pre-fix run's 5.15 GB, because pages are now stored as the JPEG bytes `apply` already produces instead of being re-encoded to PNG (the artefact noise makes pixels incompressible, so the PNG round-trip mostly inflated file size for no benefit); upload to Drive by hand. Restore on rebuild day: `tar xzf data/raw/pages-83174236e120.tar.gz -C data/processed/` then `uv run python scripts/render_sanity.py` → `ok`.

## S4 read
- T1: deps `tesserocr==2.11.0` (pinned in `pyproject.toml`; the manylinux wheel bundles libtesseract 5.5.1 — no binary, no sudo; a different wheel would change word boxes with no `read_version` change, hence the pin), `rapidfuzz`; `scripts/fetch_tessdata.sh` → `data/tessdata/eng.traineddata` (tessdata_fast, 4.1 MB, sha-pinned; `eng.traineddata` is also kept in the Drive folder as a fallback if GitHub is unreachable); `Settings.read {workers, max_tokens, tessdata_dir}`.
- T2: `models.py` `chat_full()` → `(content, finish_reason)`; fixtures store `finish_reason` (old ones replay as "stop"); image data-URL MIME from suffix; `endpoint_serves(ep)`.
- T3: `renderer`/`mini_documents` fixtures moved to `tests/conftest.py`; `mini_config_text()`; reader endpoint in mini config is the real model id + :8002 so recorded keys replay; `tests/read/conftest.py` renders the mini cohort once per session and copies `documents/`+`pages/` per test (`mini_pages`). Gotcha for `scripts/read_sanity.py`: pandas `DataFrame.style` is the Styler accessor, so `r.style` silently returns the Styler object instead of the `style` column — filter with `r[r["style"] == "typed"]`, not attribute access (`row.style` on an `itertuples()` row and `groupby("style")` are unaffected and fine).
- T4: `read/tesseract.py` — thread-local `PyTessBaseAPI`, `SetSourceResolution(150)` (JPEGs carry no DPI), `TessWord(word, conf, x0, y0, x1, y1)` half-open px.
- T5: `read/align.py` — normalise (lower, alnum only), difflib `equal` + equal-length `replace` pair 1:1; `alignment(reading_text, tess_words)` → `[{start,end,x0,y0,x1,y1}]`, `align_frac`.
- T6: `read/cer.py` — `anchored_cer(gt_words, text)`: trim reader text to the span between first/last gt-aligned word, then rapidfuzz normalised Levenshtein (letterhead/footer ignored).
- T7: `read/reader.py` — `READ_PROMPT`, `transcribe()` raises `ReaderError` on `finish_reason == "length"` or empty.
- T8: `read/stage.py` `ReadStage` (one batch = one pages partition; `transcribe` gets one retry with `repetition_penalty=RETRY_REPETITION_PENALTY` (1.3); after it a `ReaderError` (`finish_reason=length`/empty) falls back to Tesseract text with `reader_used="tesseract"`, `medgemma_text=""`, `cer_medgemma=NaN`; a clean transcript whose `align_frac < MIN_ALIGN_FRAC` (0.30 — refusal strings, self-terminated loops) also falls back but keeps `medgemma_text`; both are counted as `fallback=N`; an `httpx.HTTPError` that survives the retry, a Tesseract/IO error or a parquet write error quarantines the batch (plain re-run resumes it); `read_version = "read-" + sha256(READ_PROMPT + TESSDATA_SHA + ALIGN_RULES + model + RETRY_REPETITION_PENALTY + MIN_ALIGN_FRAC + RETRIES + DPI:PSM + max_tokens)[:12]`; stale `render_version`/`read_version`/orphans refused without `--force`) + `auditpace read [--limit] [--force] [--workers]` (exit 2: no pages / endpoint down / guard; 1: quarantine).
- T9: MedGemma fixtures for the mini cohort recorded (`AUDITPACE_RECORD=1 uv run pytest tests/read/test_replay.py`, server up): 25 files in `tests/fixtures/mini/responses/` — 24 pages plus one retry fixture (one discharge summary loops on the first attempt; its `finish_reason: length` fixture is kept so replay exercises the retry path). Re-record after a `READ_PROMPT`, reader-model, `RETRY_REPETITION_PENALTY` or `RENDER_VERSION` change.
- T10: `scripts/read_sanity.py` (gates: typed median cer_tess < 0.05, median align_frac > 0.8, fallback < 2%; samples in `logs/read_samples/`). Prereq: `ONLY=4b make serve` (≈ 3 min to ready; GPU picked by `pick_gpu.sh`, `UTIL4=0.50`). Smoke: `AUDITPACE_MOCK=0 uv run auditpace read --limit 2 --workers 4`. Full: `nohup env AUDITPACE_MOCK=0 uv run auditpace read --workers 8 > logs/read_full.log 2>&1 &`.
  - First full run of 2026-09-15 (`read-7ff39b30929e`, `RETRY_REPETITION_PENALTY=1.1`): 04:09→05:49 (≈1h41m), 300/300 batches, 0 quarantined, fallback=160/6,388 (the CLI printed 159 because two batches were already done from the smoke run) (≈2.5%, overwhelmingly `ed_clerking` pages still hitting `finish_reason=length` after the retry) — failed the sanity script's <2% fallback gate. Controller probe (three looping pages + three clean pages): 1.1 and 1.2 still looped; 1.3 broke all three probed loops (dup-8-gram ≈ 0, anchored CER 0.03–0.11) at a small clean-page accuracy cost (+0.01–0.09 CER), acceptable since the penalty only ever applies on the retry attempt. `RETRY_REPETITION_PENALTY` raised 1.1→1.3 (`src/auditpace/read/stage.py`), which changes `read_version` and required a full `--force` re-run.
  - Second full run of 2026-09-15 (post 1.1→1.3, `--force` — every partition was stale under the new `read_version`): read_version `read-6d8638c86187`, 6,388 readings / 300 batches, 0 quarantined, fallback=68 (≈1.06%, under the 2% gate), duration 08:19→10:21 (≈2h02m) at `--workers 8`; CER medians typed tess=0.0000 medgemma=0.0101, handwritten tess=0.0197 medgemma=0.0093; align_frac median 0.971. `scripts/read_sanity.py` → `ok`. Archive `data/raw/readings-83174236e120.tar.gz` (74,394,233 bytes / 71 MiB); restore: `tar xzf data/raw/readings-83174236e120.tar.gz -C data/processed/` then `uv run python scripts/read_sanity.py` → `ok`.
  - `make check` reports the 27B roles (`adjudicator`/`designer`/`reporter`) as FAIL until S5 serves them — expected, not a regression; only `reader` (4B) needs to be up for S4.
  - `--force`/`--limit` trap: after a `read_version` bump, every existing partition is stale, so a `--limit 2` smoke run refuses without `--force` (same orphan-guard message as a full run); `--force --limit 2` does **not** limit the deletion — it deletes ALL stale partitions first (per `ReadStage.run`) and then re-reads only the two batches under `--limit`, leaving the rest of `readings/` empty until a further un-limited run. If this happens by accident, restore from the latest archive (`tar xzf data/raw/readings-<hash>.tar.gz -C data/processed/`) rather than re-running the full cohort.
  - Third full run of 2026-09-15 (final-review fix wave: low-alignment fallback guard added, transport errors now quarantine instead of falling back, `read_version` widened to cover `max_tokens`/`MIN_ALIGN_FRAC`/`RETRIES`/DPI+PSM — see F1 below — `--force`, every partition was stale under the new `read_version`): `nohup env AUDITPACE_MOCK=0 uv run auditpace read --workers 8 --force > logs/read_full3.log 2>&1 &`, 14:19→16:14 (≈1h55m) at `--workers 8`. read_version `read-b91f26a17b8a`, 6,388 readings / 300 batches, 0 quarantined, fallback=77/6,388 (≈1.21%, under the 2% gate; of these 3 were the new low-alignment fallback and 74 the existing `ReaderError`/`finish_reason=length` fallback, overwhelmingly `ed_clerking`/`lab_report` pages). CER medians typed tess=0.0000 medgemma=0.0099, handwritten tess=0.0197 medgemma=0.0094; align_frac median 0.971. `scripts/read_sanity.py` → `ok`. Archive `data/raw/readings-83174236e120.tar.gz` (74,462,639 bytes / 71 MiB, protocol hash unchanged from earlier runs — only `read_version` moved); restore: `tar xzf data/raw/readings-83174236e120.tar.gz -C data/processed/` then `uv run python scripts/read_sanity.py` → `ok`.
- F1 (final-review fix wave, commits `fef846a`/`0095180`): (1) `MIN_ALIGN_FRAC = 0.30` gt-free guard — on the second full run 3 pages passed the `finish_reason`/empty guard but were garbage (a refusal "I am sorry, but I cannot fulfill your request…", a loop that stopped by itself, a 0.71-CER page) with `align_frac` 0.17–0.27 vs. a lowest legitimate 0.375; (2) `read_version` now also hashes `max_tokens`, `RETRIES`, Tesseract DPI/PSM and `MIN_ALIGN_FRAC`; (3) transport errors quarantine the batch after the retry instead of becoming permanent Tesseract-fallback rows; plus `tesserocr==2.11.0` pin, exit-1 CLI test, `--workers` help wording, docs. Third full run above is the result. Tests: 237 passed (≈ 6m25s), lint clean.
- Rebuild-day time for S4 (code and archives transferred; raw data never regenerated): **restore path ≈ 2 min** — `scripts/fetch_tessdata.sh` (≈ 5 s, or copy `eng.traineddata` from Drive), `tar xzf data/raw/readings-83174236e120.tar.gz -C data/processed/` (74 MB, seconds), `uv run python scripts/read_sanity.py` → `ok` (≈ 30 s); no model server needed. **Recompute path ≈ 2 h 20 min** — `uv pip install -e ".[serve]"` (≈ 10 min, once for S4+S5), HF licence/login (human, once), `hf download google/medgemma-1.5-4b-it` (8.1 GB, ≈ 20 s via xet), `ONLY=4b make serve` (≈ 3 min to ready), `auditpace read --workers 8` (≈ 2 h, 6,388 pages), sanity 30 s. `make test` adds ≈ 6.5 min either way.

## S5 adjudicate

Prereq: `readings` restored (`read-b91f26a17b8a`), `ONLY=27b make serve` (27B → :8003, ≈ 5 min to load; GPU via pick_gpu), `make check` → adjudicator ok. Worktree venv gotcha: `uv.lock` is gitignored so each venv resolves fresh; a fresh `uv sync --extra dev` picked `playwright 1.63`, whose Chromium build was not installed — `uv run playwright install chromium` (no sudo) before `make test`.

- T1: `Settings.adjudicate {workers: 8, max_tokens: 1024}`; 27B roles (`adjudicator`/`designer`/`reporter`) move to `http://localhost:8003/v1` in `config.yaml` + `serve_medgemma.sh` — port 8001 is a foreign process on this host.
- T2: `adjudicate/times.py` `TIME_RE`/`parse_time_quote` — canonical `HH:MM on DD/MM/YYYY` (`.`/`:`, single-digit fields).
- T3: `adjudicate/verify.py` — `normalise_with_map` (lower, `[0-9a-z]` kept, per-char raw-offset map), `find_quote` (first occurrence, `MIN_QUOTE_CHARS = 8`), `verify_evidence(kind, doc_id, quote, pages)` → `{kind, doc_id, page_id, quote, start, end, bboxes, found, verified}`; page-by-page within the cited `doc_id`; `bboxes` = alignment entries overlapping `[s, e)`; `verified ⇔ bboxes non-empty`.
- T4: `adjudicate/prompt.py` — `SYSTEM_PROMPT`, `USER_TEMPLATE`/`DOC_TEMPLATE`, `EVENT_GLOSS` (every protocol event + `died`), `REASON_GLOSS` (per-criterion reason-code glosses in the stage's own words, never `reasons.yaml` phrasings — a 4-token-run test over the rendered prompt enforces it) + a worked example, `reply_schema(criterion)` (per-criterion enums, property order `from_time, to_time, rationale, status, reason_tag, evidence, confidence` — rationale before status so guided decoding computes before it commits), `Reply` pydantic model, `DocText`.
- T5: `adjudicate/stage.py` `AdjudicateStage` — one item per `readings` partition; `cases`/`documents` loaded once per run (pandas); per case `adjudicate_one` → `_call` (one retry with `RETRY_NOTE`; `ModelJSONError`/`ValidationError` twice → `abstain` + `error`; `httpx.HTTPError` twice → batch quarantined) → verify `from_time`/`to_time`/`evidence[*]` → parse times → **computed** `status` (`hours_documented > target_hours`; `≤ 0` → abstain: same time string quoted for both events) → R9 (`reason_tag` only when computed status is breached); `verdict_frame` (nullable strings as pandas `"string"` so all-null partitions still write arrow strings); `prompt_version = "adj-" + sha256(SYSTEM_PROMPT + USER_TEMPLATE + DOC_TEMPLATE + EVENT_GLOSS + REASON_GLOSS + reply_schema + VERIFY_RULES + TIME_RE.pattern + MIN_QUOTE_CHARS + model + max_tokens + RETRIES + RETRY_NOTE)[:12]`; `_current` requires exact case_id set + this `prompt_version` + each patient's `read_version`; stale partitions refused without `--force`.
- T6: `cmd_adjudicate.py` `auditpace adjudicate [--limit] [--force] [--workers] [--protocol]` (exit 2: endpoint down unless mock / missing `cases`/`documents`/`readings` / stale-partition guard; exit 1: quarantine).
- T7: 27B mini-cohort fixtures recorded (`AUDITPACE_RECORD=1 uv run pytest tests/adjudicate/test_replay.py`, 27B up) — prompt iterated on the live model until the replay gates held (23 cases: called 19/21 end-present, computed-status agreement 19/19, time quotes found+verified 46/46, reason accuracy 3/7). The model's own arithmetic is unreliable (it compares "7.25 hours" against a 4-hour target as not greater) — `model_status` is kept for the methods section only; the stage's computed `status` is what S6 uses. Re-record after any `SYSTEM_PROMPT`/`USER_TEMPLATE`/`DOC_TEMPLATE`/gloss/schema/model change (fixture key = model + messages + schema; a `TIME_RE`/`max_tokens`/verifier change moves `prompt_version` but replays the same fixtures).
- T8: `scripts/adjudicate_sanity.py` (read-only store; per-criterion status counts, abstain split by `end_ts` present/absent, agreement + Cohen's κ vs `cases.breached`, reason accuracy vs `cases.true_reason`, `hours_ok`; evidence found/verified by kind; gates: case_id set == applicable cases, abstain|end ≤ 0.10, pooled κ ≥ 0.6, time-quote found ≥ 0.85, bboxes ⇔ verified, quarantine empty).
- T9 fix (commit `068ba63`): the first full-cohort smoke (`--limit 2`, `adj-704ba0338a9e`) abstained 4/37 end-present cases whose time quotes were correct and verified but **date-first** — S2 discharge-summary header fields (`Discharge date: 22/03/2020, 21:22`, `Admission date: 02/03/2020 (17:16)`; ≈ 570/1,385 discharge summaries write them that way) and MedGemma-4B reader reorderings (`recorded at 09:07 on 09/03/2020` → `recorded 09/03/2020 at 09:07`) — which `TIME_RE` rejected. 402/5,200 end-present cases (H2 11%, H4 12%, H6 12%) have such an alternative in their documents. `TIME_RE` gained a second alternative `DD/MM/YYYY[,] [at |(]HH:MM` (canonical first; first match in the quote wins; bare date/time still `None`) → `prompt_version` `adj-f84108475fb7`; re-smoke abstain|end 1/37. The mini cohort never hit a date-first quote, which is why Tasks 5–8 did not see it.
- Fast path (archive): `tar xzf data/raw/verdicts-83174236e120.tar.gz -C data/processed/` then `uv run python scripts/adjudicate_sanity.py` → `GATES: ok`. Seconds.
- Full run: `nohup uv run auditpace adjudicate > logs/adjudicate.log 2>&1 &`; poll `grep -c " ok," logs/adjudicate.log` (300). 2026-09-16 run (`--force`, first full run under `adj-f84108475fb7`; the two smoke partitions were re-adjudicated too): 03:53 (launched 03:50:57)→05:45 (≈ 1 h 54 min, ≈ 2.6 batches/min — well above the plan's 30–60 min estimate: the 27B serves the 8 in-flight ≈ 3.5 k-token prompts at ≈ 55 cases/min aggregate, ≈ 9 s per case) at `--workers 8`, 6,220 verdicts / 300 batches, errors=0, abstain=1,241 (of which 1,007 are H6/H4 cases with no documented end — expected, S5 design R7; 234 (4.5% of the 5,200 end-present cases) abstain for real — breakdown under "Known residuals" below), quarantined 0; status counts not_breached 3,534 / breached 1,445 / abstain 1,241. Sanity: pooled κ 0.968 (agreement 0.987 on 4,966 called rows), per-criterion κ H1 1.000 / H2 0.972 / H3 0.982 / H4 0.998 / H5 0.505 / H6 1.000, reason accuracy H1 0.331 / H2 0.556 / H3 0.667 / H4 0.824 / H5 0.632 / H6 0.768 (pooled 0.632 on 1,381 rows where both say breached) (breached without reason_tag 9.6% (139/1,445)), time-quote found from 0.979 / to 0.992, verified 0.977 / 0.986 (reason quotes found 0.990 / verified 0.987), `hours_ok` 97.7% pooled, `model_status ≠ status` 8.9% (443/4,979). `GATES: ok`. Known residuals (documented in `handoff-s6.md`, none re-run): H5 κ 0.505 — all 46 H5 disagreements are false breaches from the ICU note's *hospital*-admission time quoted as the ICU-admission time (`hours_ok` 0.676; naive H5 breach count 84 vs 39 true); 13 H6 no-follow-up rows for died-in-hospital patients called `not_breached` because `Date/time of death` was taken as event B (S6 must classify H6 by `cases.end_ts is null`); of the 234 end-present abstains, 13 are gap-guard, 156 quote a sentence without a time, 65 carry a time in a form still outside `TIME_RE` (`10:44 am on …`, `Discharge date: 25/02/2020, discharged at 06:31`, OCR-spaced dates) — a second widening would recover ≈ 60 cases for another `prompt_version` bump and ≈ 2 h run. Archive `data/raw/verdicts-83174236e120.tar.gz` (3,752,194 bytes / 3.6 MiB; 300 members; restore check into scratch → 300 partitions); upload to Drive by hand (`scripts/sync_drive.sh` only pulls).
- `prompt_version` = `adj-f84108475fb7`; any change to `SYSTEM_PROMPT`/`USER_TEMPLATE`/`DOC_TEMPLATE`/`EVENT_GLOSS`/`REASON_GLOSS`/`reply_schema`/verifier rules/`TIME_RE`/`MIN_QUOTE_CHARS`/model/`max_tokens`/`RETRIES`/`RETRY_NOTE` moves it and `--force` is required (stale partitions are refused otherwise, mirroring S4). A stale `read_version` in `readings`, or a `protocol_hash` on the partition that no longer matches the locked protocol's (e.g. a `target_hours` re-lock that keeps the same applicable case_id set), also invalidates `verdicts`.
- Traps: port 8001 is a foreign process on this host — the 27B roles are on 8003 (config.yaml, serve script). `--force` unlinks all stale partitions regardless of `--limit` (same tradeoff as S3/S4). `applies == False` cases never get a verdict row. The plan's "≈ 30–60 min" run estimate was off by 2×+ — budget ≈ 1 h 54 min for a full re-run. Design-spec §4 departures made on the live model (rationale-before-status schema order, reason extraction decoupled from the model's call, non-positive gap → abstain, `REASON_GLOSS` + worked example in the prompt hash, `died` gloss) are recorded as an "Amended 2026-09-16" note in the S5 design spec §4, and the date-first parser in §5.
- Rebuild-day time for S5 (code and archives transferred): **restore path ≈ 1 min** — `tar xzf data/raw/verdicts-83174236e120.tar.gz -C data/processed/` (3.6 MiB, seconds), `uv run python scripts/adjudicate_sanity.py` → `GATES: ok` (≈ 10 s); no model server needed. **Recompute path ≈ 2 h 05 min** — `ONLY=27b make serve` (≈ 5 min; `hf download google/medgemma-27b-text-it` first if the cache is cold, ≈ 51 GB), `make check`, `auditpace adjudicate` (≈ 1 h 54 min, 6,220 cases), sanity 10 s. `make test` adds ≈ 6m34s either way.

## S6 estimate

No model. `uv run auditpace estimate --coverage` (≈ 3.3 s wall-clock on the full 6,220-verdict cohort with `--sims 500` after the final fix wave — it was 18.4 s before Ŷ/Y were vectorised; `compute_all` alone is ≈ 1 s: 0.8 s on an open read-only store, median 0.98 s including opening a fresh one) → `data/processed/{estimates,funnel,runchart,timelost,alerts,queue,coverage}.parquet`; `uv run python scripts/estimate_sanity.py` → `GATES: ok`. The coverage-experiment gate (corrected ≥ 0.92) applies only to cells with reviewed sample size `m = min(N, max(2, round(fraction·N))) ≥ 20` — below that the corrected interval collapses onto the naive one whenever none of the m reviews shows a discrepancy, probability (1 − δ)^m with δ = P(Ŷ ≠ Y) (H5, δ ≈ 0.21: 0.39 / 0.075 / 0.009 at m = 4 / 11 / 20, matching the observed 0.59 / 0.91 / ≥ 0.96), so smaller-`m` cells are printed as `small-m (provisional): …` and excluded from the gate rather than failed (controller ruling, applied as a separate commit after the initial Task 11 run — see "Known residuals" below). The estimator flags that event as `flag = no_discrepancy_observed`. A `verdicts` table that does not cover every applicable case (an `adjudicate --limit` run) makes `estimate` exit 2 rather than estimating over a subset. Reviews are written by S7; with none, every `method` is `naive` and `flag = n_reviewed_insufficient` (confirmed on the full run: `n_reviewed = 0` for all six criteria, `method = naive`, `flag = n_reviewed_insufficient` on every row). `protocol.yaml`'s `actions:` block is hash-exempt — edit owners/actions freely, never re-lock. Invalidation: a stale `protocol_hash` on `verdicts` or `reviews` makes `estimate` exit 2.
- 2026-09-16 full run (`adj-f84108475fb7` verdicts, no reviews yet, `--sims 500`, `seed = 42`): category totals sum to 6,220 (n estimable 4,966 + n_abstain 234 + n_no_end 1,020, matching `verdicts` exactly); n_no_end splits H4 5 / H6 1,015 (the handoff-s6 table's "1,007" for H6 alone was a rough figure carried before S6 computed the real per-criterion split — the true count is 1,015 H6 + 5 H4 = 1,020 total, reconciled here and in `handoff-s7.md`). Per-criterion naive = corrected everywhere (no reviews): H1 19.3% (n 1,454), H2 29.5% (1,317), H3 28.4% (271), H4 35.4% (1,381), H5 38.4% (219), H6 38.6% (324, of the 370 H6 cases with a documented end). 9 alerts fired (6 `org_outlier` — H1 ×2, H2 ×2, H4 ×1, H6 ×1, each carrying `action`+`owner`; 1 `org_exemplar` H1; 1 `criterion_below_target` H4; 1 `top_time_lost` H6). Queue = 6,220 (no reviews yet, so every case is pending); top of queue is `abstain` H4/H2 rows with `priority = 14` (model disagreement + unverified evidence + abstain-with-end + in an alerted cell + few reviews). Coverage: naive collapses to 0.0 at every H5 fraction < 1.0 (naive breach count 84 vs true 38 on the 219 estimable H5 cases — 39 over all applicable H5 cases, one of which is an abstain — the same H5 model-arithmetic bias documented in S5); corrected H5 coverage is 0.588 (frac 0.02, m = 4 reviews), 0.910 (0.05, m = 11), then ≥ 0.964 from 0.10 up — the zero-rectifier event (no discrepancy among m reviews, probability (1 − 0.21)^m) on the criterion with the largest naive bias (the property test in `test_ppi.py` only exercises N = 2,000, frac ≥ 0.05, i.e. n ≥ 100 — this real-cohort regime at n = 4–11 was untested). H3 (m=5, m=14) and H6 (m=6, m=16) also drop below m=20 at the two lowest fractions without any coverage problem (both 1.000) — confirming the fix should gate on sample size, not single out H5 by name. All other criteria/fractions are 1.000 corrected and naive. See `handoff-s7.md` for the full per-criterion and coverage tables.
- Archive: none needed — S6 output is deterministic and ≈ 1 s to regenerate from `verdicts`/`cases`/`patients`, so nothing is archived to `data/raw/`.

## S7 workbench

No model — both vLLM servers stay off. `uv run auditpace workbench --port 8080` (pre-flight: `verdicts` + `cases` present, protocol locked, else exit 2; prints `workbench: http://127.0.0.1:8080/queue   demo: http://127.0.0.1:8080/queue?demo=1`). Run it in the background and poll: `nohup uv run auditpace workbench --port 8080 > logs/workbench.log 2>&1 &` then `curl -s http://127.0.0.1:8080/healthz` until `{"ok": true, …, "reviews": n, "load_error": null}` (≈ 3 s to first response: one `Snapshot.load` = `compute_all` on the 6,220-verdict cohort). Stop it by port, never by name: `kill $(ss -ltnp | grep ':8080' | grep -o 'pid=[0-9]*' | cut -d= -f2)`. Port 8001 is a foreign process on this host — never touch it. Tests: `make test` → **391 passed, 0 skipped** (7 m 30 s, before the final-review fix wave added two workbench tests — expect 393; includes the Playwright smoke test `tests/workbench/test_playwright_smoke.py`, marked `slow`, which skips — never fails — when Chromium is missing: `uv run playwright install chromium`), `make lint` clean. `uv run pytest tests/workbench -q` → 57 passed in ≈ 19 s.
- 2026-09-16 full-cohort check (real `data/processed/`, no reviews): `/healthz` `reviews: 0`; `GET /queue` **200 in 0.39–0.44 s** (6,220 rows, 2.9 MB HTML — the spec's "under 3 s" budget has 7× headroom, no pagination needed); `GET /results` 200 in 0.09–0.16 s; `GET /report` 9 ms; `/queue?criterion=H5` 0.18 s; `/queue?category=breached_legitimate` 0.16 s; `GET /case/{id}` 39 ms; `GET /page/{id}.jpg` 26 ms; 677 queue rows carry "model call ≠ computed status" in their `priority_reason`. `POST /reviews` (confirm on the top pending row, `11ea4e1b…:H4`, an H4 abstain at priority 14) **200 in 1.53 s** — write one partition + `Snapshot.load`; `reviews: 1`; the row left the queue; the store was then wiped (`rm -rf data/processed/reviews`) so the real store stayed clean before seeding. With the 150-review seed in place: `/queue` 0.72 s, `/results` 0.11 s, `POST /reviews` **2.3–3.1 s** (the reviews view concatenates one parquet per review — `store.read` cost grows with the review count; ≈ 150 partitions ≈ +1 s per click; compaction is a deferred item for S8+). Override of the H5 beat case (`9f78628c…:H5` → `not_breached`) moved the corrected H5 rate 29.7 % (95 % CI 16.2–43.1) → 25.9 % (10.9–40.8) while the naive 38.4 % (32.2–44.9) stayed put; that review and one more confirm were deleted by partition file afterwards, leaving exactly the seed.
- Seed for rehearsal (after `docs/demo_cases.txt` lists the beat cases — it must, because the seed excludes them and `--wipe` is the only way to un-review a case): `uv run python scripts/seed_reviews.py --per-criterion 25 --seed 42 --wipe` — **13.1 s** wall-clock for 150 reviews (≈ 87 ms per `write_review`), output `data/processed/reviews/r_<uuid>.parquet` × 150 (1.2 MB, gitignored). Printed table (n_reviewed < 25 because only estimation-pool reviews count, `estimation_pool_fraction = 0.8`):

```
H1: 25 seed reviews
H2: 25 seed reviews
H3: 25 seed reviews
H4: 25 seed reviews
H5: 25 seed reviews
H6: 25 seed reviews
criterion_id  n_reviewed method                    flag
          H1          21    ppi no_discrepancy_observed
          H2          21    ppi no_discrepancy_observed
          H3          20    ppi no_discrepancy_observed
          H4          19    ppi no_discrepancy_observed
          H5          23    ppi                    <NA>
          H6          20    ppi no_discrepancy_observed
```

  So on the Results tab every criterion except H5 is **provisional — no review has yet disagreed with a verdict** (the seed is structured truth, and the verdicts agree with it on every sampled case for H1–H4/H6), H4 is additionally provisional by `n_reviewed = 19 < 20`, and H5 alone shows a genuine corrected interval (`method = ppi`, 23 reviewed, corrected 29.7 % vs naive 38.4 % — the S5 hospital-admission-as-ICU-admission bias, corrected by 23 reviews). To make every criterion clear the 20-review floor use `--per-criterion 30`. The seed is left in place for rehearsal; wipe with `rm -rf data/processed/reviews` (or `--wipe` on the next seed).
- Demo URL: `http://127.0.0.1:8080/queue?demo=1` — the C1–C5 checklist panel (top-right, `mm:ss` clock, reset link) ticks C1 (abstain chip / unverified badge) and C3 (alert card) when the marked element scrolls into view, C2 when the queue's category select changes, C4 when a queue reason cell is clicked, C5 by hand; `?demo=1` follows every internal link. Plain `/queue` emits no panel and no script.
- Seven pitch beats (`docs/pitch.md` part 3, 110 s) — case ids from `docs/demo_cases.txt`, re-check at rehearsal; each beat's click ticks the constraint named:
  1. **Queue** (C4) — open `/queue?demo=1`; read the top row's `priority_reason` aloud (the real top row is an H4 abstain at priority 14: "model call ≠ computed status; evidence not verified on page; abstained though end event exists; in alerted cell: criterion_below_target:H4:all"); C4 ticks when a reason cell is clicked. Note "few reviews yet for Hn" disappears once a criterion has ≥ 5 estimation-pool reviews, i.e. after the seed.
  2. **Stalled** (C2, C1) — case `1f6e17d1-4c15-4397-9c4f-753e89c1d114:H4` (pick `breached — stalled` in the queue's category select — that select is the C2 marker, changing it ticks C2 — plus criterion H4, or type the id in the case-id box): `prescription_unsigned`, 160.9 h vs 24 h, "show on page" draws the rectangle on the ward-round page; category chip "breached — stalled". **Confirm** — one click; progress bar moves.
  3. **Legitimately waiting** (C2) — case `8eeabeae-aee9-4a9c-9508-e617a6c7d1a5:H4`: `contraindication_documented`, "Enoxaparin withheld pending platelet count as thrombocytopenia…", chip "breached — legitimate wait".
  4. **Missing data** (C1) — case `00f6f552-84e0-4553-a75d-f8090f02dcd0:H6` (first row of the no-end block that closes the queue — the 1,020 `abstain_no_end` rows sort after every other pending row, priority 0, "no follow-up documented — nothing to review"; the case header says end "not documented"; the chip ticks C1) and case `bf1553be-7b82-4b44-9dd0-e922a4da7515:H3` (from-time quote carries the red "not found on page — no rectangle" badge, the to-time quote draws a rectangle; header 13.8 h / 12 h from the quoted times).
  5. **Override** (C1) — handwritten SpO2 82/92 example: *case id: fill in at rehearsal* (no candidate identified from the queue data alone — needs a reading-error case found by eye). Then case `9f78628c-db81-43bd-af58-9f9b733924a7:H5`: the header shows "model call ≠ computed status (model said not_breached)", rationale "Admitted 14:56 on 27/02/2020, ventilated 20:52 on 28/02/2020: 30.25 hours" — the quoted from-time is the hospital admission, not ICU; **Override… → not breached → Save override**; Results tab: H5 corrected rate moves (29.7 % → 25.9 % on the seed), naive stays 38.4 %. The header's "measured (from quoted times)" reads 29.9 h / target 6 h — the verdict's own `hours_documented` from the quoted hospital-admission time — so the screen agrees with the status and the queue reason ("23.9 h over target"); the structured `cases` timing is never shown (controller ruling, Task 12).
  6. **Alerts** (C3) — Results tab, alert cards: H1 `37b4d73f…` org above the 3σ limit (`alarm_high`), **Action** / **Owner** from `protocol.yaml`'s `actions:` block; H6 `top_time_lost` rank 1 headline; the card scrolling into view ticks C3.
  7. **Existing workflow** (C5) — Report tab (`docs/report.md` when S8 has written it; "No report yet — run `auditpace report`" until then); tick C5 by hand.
- Stop the server when done (port 8080, see above). `logs/workbench.log` holds uvicorn output (gitignored).

## S8 report

Model: MedGemma 27B on `http://localhost:8003/v1` (`reporter` role); no other model needed. `ONLY=27b make serve`, then `make check` until `reporter: ok` (`AUDITPACE_MOCK=0 uv run auditpace models check`). On a shared GPU with less than ~70 GB free, the script's defaults (`--gpu-memory-utilization 0.92`, `--max-model-len 16384`) OOM; the fallback that starts cleanly on this host is `UTIL27=0.66 MAXLEN27=12288 EAGER27=1 make serve` (`scripts/serve_medgemma.sh`'s two new overrides — `MAXLEN27` sets `--max-model-len`, `EAGER27=1` adds `--enforce-eager`, disabling CUDA graphs to trade throughput for a smaller memory footprint; the 4B block is unchanged). Stop by port when done, never by name: `kill $(ss -ltnp | grep ':8003' | grep -o 'pid=[0-9]*' | cut -d= -f2)` — port 8001 is a foreign process on this host, never touch it.

Run `uv run auditpace estimate --coverage` first whenever `reviews` changed since the last `estimate` — the workbench's per-click recompute is in memory only and never writes `estimates.parquet`, so a stale `estimates` table would make `report` describe yesterday's screen. Full-cohort timing: **3.8 s**. `auditpace report` does not call `compute_all` itself (spec §8): it reports exactly what `estimate` last wrote and prints `estimates.computed_ts` in Methods.

`uv run auditpace report --no-model --out docs/report.fallback.md` (**4.2 s**) is the pitch fallback: no model call, every prose paragraph is the fixed "Narrative withheld: report writer unavailable." sentence, and the report is otherwise complete — six criteria, all tables, all figures, every caveat, the seed-reviewer note, the validation section with H5 named. `cp docs/report.fallback.md docs/report.md` if the model misbehaves on the day. Both files are gitignored.

`AUDITPACE_MOCK=0 uv run auditpace report` writes `docs/report.md` and `data/processed/report/<run_id>/{payload.json,prose.json,gateway.log,report.md,org_map.json}`, and appends to the run-spanning `data/processed/report/gateway.log`. This run's `gateway.log` line statuses, in order: `sent` (model, prompt_version, sha256 and `n_bytes` of the *final* payload that was or would be sent — 111,328 bytes on the full cohort; this is `payload.model_dump_json()`, the full model, not the compact view below), `payload` (the full payload, logged right after), then either `prose_rejected` per back-checked paragraph or `model_unavailable`. (Fix wave 2, item 9: this order — `sent` before `payload` — is what the code writes; an earlier revision of this section had the two reversed.)

**Fix wave 1 (2026-09-17): the compact `for_model()` view fixed the context-length residual below.** The original full-cohort run (Task 12, 2026-09-16/17) found `Payload.for_model()` sending the model a **79,298-token** prompt (166 KB indented JSON, `model_dump()` minus four volatile run fields) — 6.5× the reporter's 12,288-token context — so every real-model call returned `400 Bad Request` and every run fell back to `model_unavailable`. The controller ruled `for_model()` a compact, pre-formatted view instead (spec §5 amendment): every rate becomes one string via `fmt.pct`/`fmt.ci` (e.g. `"28.6 % (10.0–50.0 %)"`), `validation` is dropped, and — the dominant cut — organisation *segment* rows are dropped per criterion (the per-run bulk that scaled with cohort size), with the organisation signal surviving only as `organisation_outliers` from the already-filtered `funnel` rows. `prose.user_message()` also switched from indented to compact JSON (`ensure_ascii=False`, no whitespace) after the indented form put the real prompt exactly one token over budget (10,241 vs a 10,240-token allowance at the old `max_tokens=2048`); and `stage.MAX_TOKENS` was raised 2048 → 3072, because a 6-criterion reply (18 `where`/`why`/`who` paragraphs plus summary/actions_note/caveats_note) needs more than 2048 completion tokens to finish the guided-JSON reply — the first real run under the compact prompt got `model_unavailable` from `finish_reason=length`, not a context overflow. Measured after the fix, on the same real cohort: **prompt 8,128 tokens** (`POST /v1/chat/completions` with `max_tokens=1` isolates `usage.prompt_tokens`; a 90 % cut from 79,298), prompt chars 19,219 (user message) + 2,297 (system prompt) = 21,516 (down from 166 KB). `docs/superpowers/specs/2026-09-17-s8-report-design.md` §5 carries the full amendment.

- **2026-09-17 full run, before the fix** (`estimate --coverage` run after the 150-review seed from S7; no reviews added since): six criteria, `method = ppi` everywhere; H1/H2/H3/H4/H6 provisional (`flag = no_discrepancy_observed`, wording "no review has yet disagreed with a verdict — the corrected interval is the naive one relabelled"); H5 alone not provisional (23 reviewed, corrected 29.7 % (16.2–43.1 %) vs naive 38.4 % (32.2–44.9 %)). 11 alerts. Payload 111,328 bytes; 86 organisations pseudonymised `Org-01`…`Org-86`. All 24 prose fields fell back to the fixed sentence — 0 accepted, `model_unavailable` on every run, never a back-check `prose_rejected`.
- **2026-09-17, after the fix** (same seeded cohort, unchanged since the "before" run — no new reviews): two consecutive `AUDITPACE_MOCK=0 uv run auditpace report` runs, **1m46s** then **1m49s** wall-clock (guided-JSON decoding on a shared, contended GPU — `user`/`sys` time alone is ≈ 6-7 s; the difference is queueing, not the fix). Printed summary line both times: `report: run_id=<id> sent=1 rejected=0 prose_rejected=1 model=google/medgemma-27b-text-it → docs/report.md`. `prose.json` both runs: **20 of 21 paragraphs accepted**, 1 rejected — `{"section": "actions_note", "tokens": [], "reason": "over length"}` (the model's actions_note ran past 80 words × 1.2; a legitimate back-check hit, not a token-membership failure, and not something this fix wave's payload change touches). `prompt_version rpt-53f30a560b75` (run 1). The second run's "Change since previous run" section named the first run by id and showed **zero deltas** on all six criteria (Δ pp = +0.0 throughout, reviewed and provisional unchanged) and every alert "persisting" — exactly the expected idempotence with no reviews added in between.
- **What read wrong in the accepted prose** (read `docs/report.md` in full, 1,079 lines): (1) several criteria's `where` paragraph calls the criterion's total case count "reviewed cases" — e.g. H1's prose says "based on 1454 reviewed cases" when the table two lines above it says "Reviewed 21 / 1454"; `1454` is a legitimate printed count token (`c.n`), so the back-check accepts it, but the claim conflates the model-estimated cohort size with the clinician-reviewed count. (2) H5's `where` paragraph quotes the **naive** rate (38.4 %) as "the" breach rate rather than the **corrected** one (29.7 %, 16.2–43.1 %) — H5 is the one criterion in this cohort where naive and corrected diverge (the S5 hospital-admission-as-ICU-admission bias), so this is exactly the criterion where picking the wrong figure is most misleading; both numbers are valid payload tokens, so the back-check has no way to prefer one. (3) the accepted `caveats_note` states "The provisional reason for H1, H2, H3, H4, and H5 indicates that…" — factually wrong: H5 is the one *non*-provisional criterion this run (see the Summary table's "no"), and H6 (provisional) is omitted instead. All three are instances of the pre-existing "context-blind back-check" limitation already logged in `docs/superpowers/handoff-s9.md`'s deferred minors (`check_prose` verifies a token exists somewhere in the payload's printed set, not that it is attributed to the right criterion or claim) — now observed live for the first time, since every earlier real run fell back to placeholder text before any prose reached the back-check. Nothing else read wrong: six criteria in the right order, correct provisional flags in the summary table and Caveats bullets, seed caveat present, H6 "1,015 no follow-up documented" wording present (as "1020" cohort-wide plus the per-criterion table), validation section present with H5 named, only `Org-nn` values, no UUIDs, Gateway line correctly reporting "20 of 21 narrative paragraphs accepted, 1 paragraph withheld (actions_note)".

`uv run python scripts/report_sanity.py` (no model; run after `estimate --coverage` so the printed `n_reviewed`/`method` reflect the seed, not a stale pre-review `estimates` table) → `GATES: ok`, printing `payload: 111,260 bytes; 6 criteria; 11 alerts; 86 organisations pseudonymised; 777 printed tokens`, one line per criterion (`n`, `n_reviewed`, `method`, `provisional_reason`), `reviewers: ['seed']`, `validation: present`.

Workbench check (Report tab, pitch beat 7): `nohup uv run auditpace workbench --port 8080 > logs/workbench.log 2>&1 &`, poll `/healthz`, then `curl -s http://127.0.0.1:8080/report | grep -c '<h2>'` → **9** once `docs/report.md` exists ("No report yet — run `auditpace report`" before it does; no live reload needed, the tab reads the file on every request). Gotcha hit in this session: a workbench process from an earlier task in the same worktree was still bound to `:8080` from several hours before this run and silently absorbed the `nohup` start — `logs/workbench.log` logged `[Errno 98] address already in use` on the *new* attempt while the *old* process kept answering `/healthz` with a stale `loaded_ts`, so `/report` still said "No report yet" even after `docs/report.md` existed. Check `ss -ltnp | grep :8080` for a pid whose `ps -o etimes=` predates this session before trusting a `nohup` start actually replaced anything; kill it by port and restart. Stop by port when done, same rule as `:8003` — never by name.

`docs/rebuild.md`'s "S8 report" runbook is now this section; `scripts/report_sanity.py` is the gateway-fuzz-and-no-leak check against the real store (spec §10's last bullet).

- **Fix wave 2 (2026-09-18, final review, commit `d3d04ab`; branch merged to `main` @ `8d5c210`, worktree removed):** `FALLBACK_LENGTH` names over-length rejections separately from token rejections; `PROPORTION_RE` rejects "1 in 3"/"nine in ten"; `NUMBER_WORD_EXTRA_RE` (dozen/tenth…) and case-insensitive `CRIT_RE`/`ORG_RE`/`ID_RE`, all kept out of `prompt_version`'s blob so the recorded 27B fixture still replays; raw `ProseUnavailable` text never reaches `report.md` (fixed `model_note`, detail in `prose.json`/gateway log); `org_map_changed` computed from the previous run's `org_map.json` and printed under the comparison table; summary line carries `model_unavailable=1|0` and the CLI warns on stderr; `verdicts` hash-checked (its `protocol_hash` column only) before the stage runs; comparison table ends cleanly before "New alerts"; withheld bullets print each rejection's reason; `generated_ts` labelled UTC; explicit utf-8 on every report file. `uv run pytest tests/report` → **76 passed**; full suite collects **469**. Known residuals carried to S9 (`handoff-s9.md`): `actions_note` is consistently over its 80-word limit in live runs (rejected → fallback sentence), and the accepted prose still "reads wrong" in the three context-blind ways listed above (the back-check verifies figures, not claims).

## S9 / S10 — next (not for the pitch)

*Checkbox list: [docs/superpowers/todo-s9-s10.md](superpowers/todo-s9-s10.md).*

Ruling 2026-09-17 (`handoff-s8.md` "S9 / S10 scope", `handoff-s9.md`): the 2026-09-18 pitch runs on the cohort as it stands. **S9** = systematic audit of every verdict-failure class on the full cohort — one script `scripts/verdict_failure_audit.py` → `data/processed/failure_audit` (one row per verdict × failure class, `recoverable_by` ∈ {regex, verify_fallback, docid_fallback, prompt, model, human}) + a workbench filter on it. Six classes already measured: am/pm times unparsed (19 of 1,292 `None` quotes), non-verbatim quotes (144 unfound cites, 82 recoverable by timestamp-substring fallback, 35 rewritten into the few-shot phrasing), doc_id transpositions (64, quote verbatim on the real page), few-shot leakage (2), under-called breaches (392 model-not_breached vs computed-breached; split by cause — the H5 hospital-vs-ICU admission bias first), OCR-garbled timestamps. **S10** = implement what S9 ranks; objective: minimise clinician review tasks at a fixed corrected-rate CI width; success metric: pending-queue size and mean priority before vs after on the same seed. Constraints: code/regex fixes first (no model run needed); MedGemma 4B stays the vision page reader (the served 27B is text-only); any prompt change to S5 costs a `prompt_version` bump and a ≈ 2 h 27B re-run; any change to `verdicts` invalidates `estimates`/`report` (re-run steps 6–9 above). Start S9 with `/superpowers:brainstorming` — scope is open.

